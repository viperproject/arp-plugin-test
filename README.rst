Test runner for the `ARP Plugin<https://bitbucket.org/viperproject/arp-plugin/>`_.

To run tests, checkout this repository to the same folder as ``silver``,
``silicon``, ``carbon``, and ``arp-plugin`` and run ``sbt test``.
